navegue no terminal ate este diretorio e ultilize o cmd.txt para rodar os codigos de treino e predição.

a arquietura do modelo esta no diretorio pyimagesearch nomeada como SmallVGGNet.py.

bibliotecas necessarias:

pip install sklearn
pip install tensorflow
pip install keras
pip install matplotlib
pip install numpy
pip install argparse
pip install opencv-python
pip install pickle
